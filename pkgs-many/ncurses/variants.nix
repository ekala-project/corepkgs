{
  # Note: All variants use the same source version (6.5).
  # The difference is in the ABI (Application Binary Interface) version,
  # which affects library naming and compatibility with existing binaries.

  v5 = {
    abiVersion = "5";
  };

  v6 = {
    abiVersion = "6";
  };
}
